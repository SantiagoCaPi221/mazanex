apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: mazanex
spec:
  selector:
    app: ms-auth
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
      