apiVersion: v1
kind: Service
metadata:
  name: mazanex-auth
  labels:
    app: mazanex-auth
spec:
  type: ClusterIP
  selector:
    app: mazanex-auth
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      name: http
