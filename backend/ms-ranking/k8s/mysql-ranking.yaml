apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-ranking
  namespace: mazanex
spec:
  selector:
    matchLabels:
      app: mysql-ranking
  template:
    metadata:
      labels:
        app: mysql-ranking
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        env:
        - name: MYSQL_DATABASE
          value: db_ranking
        - name: MYSQL_ROOT_PASSWORD
          value: root
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-ranking-service
  namespace: mazanex
spec:
  selector:
    app: mysql-ranking
  ports:
  - port: 3306