name: CI - Ranking Service

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - 'ranking-service/**'
  pull_request:
    branches: [ "main", "develop" ]
    paths:
      - 'ranking-service/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./ranking-service

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Configurar Java 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Ejecutar pruebas de Ranking
      run: mvn clean test