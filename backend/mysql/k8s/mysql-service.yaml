apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mazanex
  labels:
    app: mazanex-mysql
spec:
  selector:
    app: mazanex-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  type: ClusterIP
