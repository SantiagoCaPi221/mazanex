apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: mazanex
spec:
  type: LoadBalancer # Permite acceso externo
  selector:
    app: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80